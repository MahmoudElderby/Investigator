# Orchestrator Memory

Cross-cutting orchestration lessons. Dated bullet entries.

## Reusable how-to

When a case **needs** a join, fetch path, or query shape, look here first. Do not rediscover names the kit already learned. If a case does not need a join, ignore this table.

| When needed | What | Where / how | Learned in |
|-------------|------|-------------|------------|
| <!-- e.g. Join provider webhook to our DTO/SQL --> | <!-- e.g. provider requestID ↔ requestIdHash --> | <!-- file, attribute, column, query --> | <!-- case-id --> |

<!-- Append rows on case close only when something new was discovered. -->

## Lessons

<!-- Append dated bullets below -->
